/**
 * Owner of StarPay's own records: the platform bot (hosted mode) and the
 * platform ledger accounts. Better-Auth organization IDs are random 32-char
 * strings, so no merchant can collide with it.
 */
export const PLATFORM_ORG_ID = "platform";
