# @import(../../apps/server/, pick=[NODE_ENV, CORS_ORIGIN, BETTER_AUTH_SECRET, BETTER_AUTH_URL, ENCRYPTION_KEY, PUBLIC_API_URL, WORKER_ENABLED])
# @defaultRequired=false
# @defaultSensitive=true
# ---
ALCHEMY_PASSWORD=

# Prisma Console → your workspace → Settings → Service tokens. Put it in packages/infra/.env.
# @sensitive
PRISMA_SERVICE_TOKEN=
