# @import(../../apps/server/, pick=[NODE_ENV, CORS_ORIGIN, BETTER_AUTH_SECRET, BETTER_AUTH_URL, ENCRYPTION_KEY, PUBLIC_API_URL, WORKER_ENABLED, PLATFORM_ADMIN_USER_IDS, STAR_USD_RATE, TON_PAYOUT_MNEMONIC_LIVE, TON_PAYOUT_MNEMONIC_TEST, TONCENTER_API_KEY])
# @defaultRequired=false
# @defaultSensitive=true
# ---
ALCHEMY_PASSWORD=

# Prisma Console → your workspace → Settings → Service tokens. Put it in packages/infra/.env.
# @sensitive
PRISMA_SERVICE_TOKEN=
